prefect-version: 2.19.0

build: null
push: null

deployments:
  - name: death-metal-pipeline
    version: null
    tags: [ "death-metal", "data-pipeline", "etl" ]
    description: "Pipeline de dados Death Metal Apoena - DuckDB/BigQuery"
    entrypoint: workflows/dlt_pipeline.py:death_metal_workflow
    parameters: { }
    work_pool:
      name: default

  - name: death-metal-clean
    version: null
    tags: [ "death-metal", "clean", "dlt", "dbt" ]
    description: "Transformação de dados Death Metal Apoena - DuckDB/BigQuery"
    entrypoint: workflows/dbt_pipeline.py:death_metal_dbt_flow
    parameters: { }
    work_pool:
      name: default

project:
  recipe: null

variables:
  ambiente: "desenvolvimento"
  log_level: "INFO"

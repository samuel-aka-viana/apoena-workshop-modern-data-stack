prefect-version: 2.19.0

build: null

push: null

pull:
  - prefect.deployments.steps.set_working_directory:
      directory: "/home/smovisk/PycharmProjects/apoena/workflows"

deployments:
  - name: death-metal-simple-workflow
    version: null
    tags: [ "death-metal", "simple", "dlt-pipeline" ]
    description: "Workflow simples para executar pipeline dlt Death Metal"
    entrypoint: dlt_pipeline.py:death_metal_workflow
    parameters: { }
    work_pool:
      name: default

  - name: dbt-death-metal
    version: null
    description: "Workflow dbt"
    entrypoint: dbt_prefect_pipeline.py:death_metal_dbt_flow
    work_pool:
      name: default

project:
  recipe: null

variables:
  ambiente: "desenvolvimento"
  log_level: "INFO"